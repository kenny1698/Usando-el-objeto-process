<style>
    .table td,
    .table th {
        vertical-align: middle;
    }
</style>

{{#if productosFakes.length}}
<div class="table-responsive">
    <table class="table table-dark">
        <tr>
            <th>Nombre</th>
            <th>Precio</th>
            <th>Link URL</th>
        </tr>
        {{#each productosFakes}}
        <tr>
            <td>{{this.nombre}}</td>
            <td>{{this.precio}}</td>
            <td><img src="{{this.url}}" width="60" height="60"></td>
        </tr>
        {{/each}}
    </table>
</div>
{{/if}}